<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewmodels="clr-namespace:Open_Hands.ViewModels"
             mc:Ignorable="d"
             x:Class="Open_Hands.Views.LoginPage"
             Shell.NavBarIsVisible="False">
    
    <ContentPage.BindingContext>
        <viewmodels:LoginViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>        
        <StackLayout Padding="10,0,10,0" VerticalOptions="Center">
            <Label Text="Welcome to Open Hands" HorizontalOptions="CenterAndExpand"/>
            <Entry x:Name="Email" Text="{Binding Email}" Placeholder="Email" Keyboard="Email"/>
            <Entry x:Name="Password" Text="{Binding Password}" Placeholder="Password" IsPassword="True"/>
            <Button VerticalOptions="Center" Text="Login" Command="{Binding LoginCommand}"/>
            <Label Text="-------OR-------" HorizontalOptions="CenterAndExpand"/>
            <Button VerticalOptions="Center" Text="Create Account" Command="{Binding NewAccountCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>